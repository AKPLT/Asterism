<Window x:Class="Asterism.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:Asterism.Client.Views"
        mc:Ignorable="d"
        Title="Asterism" Height="640" Width="1000">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- ヘッダー: タイトル + 検索 + 更新確認 -->
        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Asterism" FontSize="22" FontWeight="Bold" VerticalAlignment="Center" DockPanel.Dock="Left" />
            <Button Content="管理者モード" DockPanel.Dock="Right" Padding="10,4" Margin="8,0,0,0"
                    Click="OnAdminModeClick" />
            <Button Content="更新を確認" DockPanel.Dock="Right" Padding="10,4" Margin="8,0,0,0"
                    Command="{Binding RefreshManifestCommand}" />
            <TextBlock Text="検索:" DockPanel.Dock="Left" Margin="24,0,4,0" VerticalAlignment="Center" />
            <TextBox x:Name="SearchBox" Margin="0,0,0,0" Padding="6,4" VerticalContentAlignment="Center"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
        </DockPanel>

        <!-- バナー -->
        <Border Grid.Row="1" Margin="0,0,0,8" Padding="8" CornerRadius="4"
                Visibility="{Binding BannerMessage, Converter={StaticResource NullToVisibilityConverter}, FallbackValue=Collapsed}">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#FFF3CD" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsBannerError}" Value="True">
                            <Setter Property="Background" Value="#F8D7DA" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="{Binding BannerMessage}" TextWrapping="Wrap" />
        </Border>

        <!-- 本体: カテゴリサイドバー + カード一覧 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ListBox Grid.Column="0" Margin="0,0,8,0"
                     ItemsSource="{Binding Categories}"
                     SelectedItem="{Binding SelectedCategory}" />

            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding ToolsView}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <views:ToolCardView />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
