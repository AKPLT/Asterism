<Window x:Class="Asterism.Client.Views.AdminToolEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" Height="620" Width="480"
        WindowStartupLocation="CenterOwner">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="16">
        <StackPanel>
            <TextBlock Text="パッケージファイル（新規登録時は必須。編集時は未選択なら既存ファイルを維持）" FontWeight="Bold" TextWrapping="Wrap" />
            <DockPanel Margin="0,2,0,10">
                <Button Content="選択..." DockPanel.Dock="Right" Padding="8,4" Click="OnSelectPackageClick" />
                <TextBlock Text="{Binding PackageFilePath}" VerticalAlignment="Center" Margin="0,0,8,0" TextTrimming="CharacterEllipsis" />
            </DockPanel>

            <TextBlock Text="ID" FontWeight="Bold" />
            <TextBox Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsIdReadOnly}" Margin="0,2,0,10" Padding="4" />

            <TextBlock Text="名前" FontWeight="Bold" />
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,10" Padding="4" />

            <TextBlock Text="バージョン" FontWeight="Bold" />
            <TextBox Text="{Binding Version, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,10" Padding="4" />

            <TextBlock Text="説明" FontWeight="Bold" />
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,10" Padding="4"
                     TextWrapping="Wrap" AcceptsReturn="True" Height="60" />

            <TextBlock Text="カテゴリ" FontWeight="Bold" />
            <TextBox Text="{Binding Category, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,10" Padding="4" />

            <TextBlock Text="タグ（カンマ区切り）" FontWeight="Bold" />
            <TextBox Text="{Binding TagsText, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,10" Padding="4" />

            <TextBlock Text="配布形式" FontWeight="Bold" />
            <ComboBox ItemsSource="{Binding PackageTypeValues}" SelectedItem="{Binding PackageType}" Margin="0,2,0,10" Padding="4" />

            <TextBlock Text="実行ファイルパス（Zip型:相対パス / Installer型:絶対パス、環境変数展開可）" FontWeight="Bold" TextWrapping="Wrap" />
            <TextBox Text="{Binding ExecutablePath, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,10" Padding="4" />

            <TextBlock Text="インストーラー引数（Installer型のみ、任意）" FontWeight="Bold" />
            <TextBox Text="{Binding InstallerArgs, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,10" Padding="4" />

            <TextBlock Text="アイコンファイル（任意。未選択なら既存アイコンを維持）" FontWeight="Bold" TextWrapping="Wrap" />
            <DockPanel Margin="0,2,0,10">
                <Button Content="選択..." DockPanel.Dock="Right" Padding="8,4" Click="OnSelectIconClick" />
                <TextBlock Text="{Binding IconFilePath}" VerticalAlignment="Center" Margin="0,0,8,0" TextTrimming="CharacterEllipsis" />
            </DockPanel>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,4,0,10"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="保存" Width="80" Command="{Binding SaveCommand}" />
                <Button Content="キャンセル" Width="80" Margin="8,0,0,0" Click="OnCancelClick" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Window>
